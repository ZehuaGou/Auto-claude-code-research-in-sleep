{
  "topic": "Token-level hallucination detection via hidden state trajectory anomaly detection",
  "search_intent": "novelty_check",
  "must_include": [
    "hallucination detection",
    "hidden states",
    "token-level hallucination",
    "representation trajectory",
    "LLM internal states"
  ],
  "exclude": "generic anomaly detection unrelated to language models",
  "sources": [
    "openalex"
  ],
  "time_range": {
    "start_year": 2020,
    "end_year": 2026
  },
  "max_results_per_source": 10,
  "query_variants": [
    "Token-level hallucination detection via hidden state trajectory anomaly detection",
    "Token-level hallucination detection via hidden state trajectory anomaly detection hallucination detection hidden states token-level hallucination representation trajectory LLM internal states",
    "Token-level hallucination detection via hidden state trajectory anomaly detection hallucination detection",
    "Token-level hallucination detection via hidden state trajectory anomaly detection hidden states",
    "Token-level hallucination detection via hidden state trajectory anomaly detection token-level hallucination",
    "Token-level hallucination detection via hidden state trajectory anomaly detection representation trajectory",
    "Token-level hallucination detection via hidden state trajectory anomaly detection LLM internal states",
    "hallucination detection hidden states",
    "hallucination detection token-level hallucination",
    "hallucination detection representation trajectory"
  ],
  "notes": "Generated by build-search-plan (no network, no model). Query variants are deterministic and non-exhaustive."
}